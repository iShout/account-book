import {
  View,
  FlatList,
  DeviceEventEmitter,
  Text,
  Image,
  TouchableOpacity,
} from 'react-native';

const StatisticPage = () => {
  return <Text>STATISTIC PAGE</Text>;
};

export default StatisticPage;
