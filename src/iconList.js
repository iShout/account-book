const imageRoute = './images/';
const book = require(`${imageRoute}book.png`);
const chaos = require(`${imageRoute}chaos.png`);
const code = require(`${imageRoute}code.png`);
const commentBubble = require(`${imageRoute}comment-buble.png`);
const drink = require(`${imageRoute}drinks.png`);
const fireFolder = require(`${imageRoute}fire-folder.png`);
const gift = require(`${imageRoute}gift.png`);
const lock = require(`${imageRoute}lock.png`);
const unknown = require(`${imageRoute}unknown.png`);
const unlock = require(`${imageRoute}unlock.png`);
const iconList = [
  book,
  chaos,
  code,
  commentBubble,
  drink,
  fireFolder,
  gift,
  lock,
  unknown,
  unlock,
];
export default iconList;
